<!-- Build table of sections -->
<h2><%=t('.page_title') %></h2>
<%=render 'admin/adminNavbar' %>
<strong><%= link_to t('.new_button'), [:new, :admin, :section]%></strong>
<table>
  <thead>
    <tr>
      <th><%=t('.sect_title') %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <%= render @sections %>
  </tbody>
</table>

